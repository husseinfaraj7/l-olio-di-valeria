window.emailConfig = {
  getOrderEmailParams(orderDetails = {}) {
    console.info('[Placeholder] Generating order email parameters.', orderDetails);
    return orderDetails;
  },
  loadUserPreferences() {
    console.info('[Placeholder] Loading user preferences.');
    return {};
  }
};
